const express = require(express);
const router = express.Router();


//contains name and style -- style can be an array of one or all of : (anime, semi-realism, painterly)

//this will be used for the admin to add new types: so we do not need an :id identifier or any query stuff. 
router.get("/", async (req, res) => { 

})

router.post("/", async (req, res) => { 

})

router.put("/", async (req, res) => { 

})

router.patch("/", async (req, res) => { 

})

router.delete("/", async (req, res) => { 

})