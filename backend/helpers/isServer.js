const express = require(express);
const session = require(express - session);

//check if the user requesting is the server or is authorized to make such requests. 
async function isServer(req) {
     
}
