const express = require(express);
const mongoose = require(mongoose);
const { Schema } = mongoose;
const { commission, commissionType, addOns } = require("../models/commission");

//TODO: unfinished

async function determineAddOnValid(comm) {
  //each commission will have a commission type associated with it and add ons
  //check if add ons match the commission type
 

}
